import {Component, h, Host} from '@stencil/core';

@Component({
  tag: 'verdocs-loader',
  styleUrl: 'verdocs-loader.scss',
  shadow: false,
})
export class VerdocsLoader {
  render() {
    return <Host />;
  }
}
