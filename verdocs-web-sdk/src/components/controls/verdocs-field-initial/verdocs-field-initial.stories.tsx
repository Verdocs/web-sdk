import {html} from 'lit-html';
import {Meta} from '@storybook/web-components';

export default {
  title: 'Controls/Document Fields/Initial',
  component: 'verdocs-field-initial',
  args: {},
  argTypes: {},
} as Meta;

export const Initial = ({}) => html`<verdocs-field-initial />`;
